<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Ticket #<%= @ticket_id %></title>
    <style>
      body {
        font-family: sans-serif;
      }
    </style>
  </head>
  <body style="text-align: center;">
    <%= image_tag wicked_pdf_asset_base64('ticket.jpg'), style: "height: 125px;" %>
    <h2><strong>Ticket Caja 1 #<%= @ticket.id %></strong></h2>
    <% if @ticket.burger_quantity.positive? %>
      <p><%= @ticket.burger_quantity %> hamburguesas de carne</p>
    <% end %>
    <% if @ticket.veggie_quantity.positive? %>
      <p><%= @ticket.veggie_quantity %> hamburguesas veggie</p>
    <% end %>
    <% if @ticket.drink_quantity.positive? %>
      <p><%= @ticket.drink_quantity %> bebidas</p>
    <% end %>
    <hr>
    <p><strong><%= @ticket.tokens %> tokens</strong></p>
    <hr>
    <p>Ticket creado a las <%= @ticket.created_at.in_time_zone("America/Santiago").strftime("%H:%M") %></p>
  </body>
</html>
